import React from 'react';
import { UserDetails } from '../@Types/UserDetails';
import pattern from "../assets/pattern.png";

const RoastCard = React.forwardRef<HTMLDivElement, UserDetails>(
  ({ username, avatarUrl, postText, error }, ref) => {
    return (
      <div
        ref={ref}
        className="position-relative bg-primary bg-gradient text-white p-4 shadow rounded-4"
        style={{
          width: '1080px',
          height: '1080px',
          overflow: 'hidden',
          backgroundImage: `url(${pattern})`,
          backgroundSize: 'cover',
          backgroundPosition: 'center',
        }}
      >
        <div className="d-flex flex-column align-items-center justify-content-center h-100 text-center position-relative" style={{ zIndex: 1 }}>
          <img
            src={avatarUrl || 'https://via.placeholder.com/400/333333/cccccc?text=CF'}
            alt="Avatar"
            className="rounded-circle border border-light shadow mb-4"
            style={{ width: '256px', height: '256px', objectFit: 'cover' }}
          />
          <h2 className="display-4 fw-bold">{username || 'Anonymous Coder'}</h2>
          <p className="fs-4 mb-4">Handle Rating: {error ? 'CE' : '404'}</p>

          <div className="bg-dark bg-opacity-75 border border-light rounded p-4 w-75 mb-4">
            <p className="fs-3">{postText || 'This roast is so weak it got TLE'}</p>
          </div>

          <div className="position-absolute bottom-0 w-100 text-center pb-3">
            <p className="fs-5">
              Generated by <strong className="text-warning">RoastForce</strong>
            </p>
            <p className="text-muted small">roastforce.gg</p>
          </div>
        </div>
      </div>
    );
  }
);

export default RoastCard;
